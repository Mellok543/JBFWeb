spring:
  datasource:
    url: jdbc:h2:mem:jbf_web_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

jwt:
  secret: "test-only-secret-key-at-least-32-bytes-long!!"
